model Subject {
  id          String           @id @default(uuid())
  name        String
  icon        String
  description String
  imageUrl   String
  color       String
  courses     Course[]
  teachers    TeacherProfile[]

  @@map("subjects")
}
